<%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="/css/forms.css">
<link rel="stylesheet" href="/css/product.css">
</head>

<body>
<%- include('../includes/navigation.ejs') %>

<main>

    <% for(var i=0; i<product_list.length; i++) {%>
        <br/>  <%= i %> 
        <%#  will output the numbers 1-10 %>
        <%= product_list[i]['title'] %>
    <% } %>

    <!-- <script>
        var list = document.getElementById('dynamicList');
        for (var i = 0; i < product_list.length; i++){
            var x = "<div class='card__item'><img src=" + product_list[i].image + " alt=" + product_list[i].title + "><h1>" + product_list[i].title + "</h1><p class='price'>" + product_list[i].price + "</p><p class='quantity'>" + product_list[i].quantity + "</p><p><button>Add to Cart</button></p></div>";
            list.innerHTML = x;
        }
    </script> -->

    <!-- <form class="product-form" action="http://localhost:3000/admin/add-product" method="POST">
        <div class="form-control">
            <label for="title">Title</label>
            <input type="text" name="title" id="title" value="<% if (editing) { %><%= product.title %><% } %>">
        </div>
        <div class="form-control">
            <label for="image">Image</label>
            <input type="text" name="image" id="image" value="<% if (editing) { %><%= product.image %><% } %>">
        </div>
        <div class="form-control">
            <label for="price">Price</label>
            <input type="number" name="price" id="price" step="0.01" value="<% if (editing) { %><%= product.price %><% } %>">
        </div>
        <div class="form-control">
            <label for="price">Quantity</label>
            <input type="number" name="quantity" id="quantity"  value="<% if (editing) { %><%= product.quantity %><% } %>">
        </div>
        <% if (editing) { %>
            <input type="hidden" value="<%= product.id %>" name="productId">
        <% } %>

        <button class="btn" type="submit"><% if (editing) { %>Update Product<% } else { %>Add Product<% } %></button>
    </form> -->
</main>
<%- include('../includes/end.ejs') %>